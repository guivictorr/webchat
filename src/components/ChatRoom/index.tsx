import React from 'react';
import SignOutButton from '../SignOutButton';

const Chat: React.FC = () => {
  return (
    <div>
      <SignOutButton />
      <h1>Chat</h1>
    </div>
  );
};

export default Chat;
